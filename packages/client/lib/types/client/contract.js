/** Shared contracts for the terminal shell plugin. */
/** Initial PTY status before any interaction. */
export const IDLE_PTY_STATUS = { id: null, state: 'idle', error: null };
//# sourceMappingURL=contract.js.map